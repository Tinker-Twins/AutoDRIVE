using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NavmeshRandomDestination : MonoBehaviour
{

    // public NavmeshAgent NavmeshAgent; // `NavMeshAgent` instance

    // private int layer_mask = 1 << 21; // Mask the `Terrain` layer to allow raycasting only against it

    // void Update()
    // {
    //     if(NavmeshAgent.Agent.pathStatus != UnityEngine.AI.NavMeshPathStatus.PathComplete)
    //     {
    //         RaycastHit hit; // Instantiate a raycast hit object
    //         if(Physics.Raycast(transform.position, Vector3.down, out hit, 1000, layer_mask))
    //         {
    //             NavmeshAgent.Agent.destination = hit.point;
    //         }

    //     }
    // }
}
